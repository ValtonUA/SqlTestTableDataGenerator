# SqlTestTableDataGenerator
Console applications. Unit tests (test-driven approach). SOLID principles.

The program generates insert scripts with random test data for concrete tables (presented by model classes).
